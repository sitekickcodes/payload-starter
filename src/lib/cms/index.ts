/**
 * CMS layer — frontend pages import from here, never directly from Payload.
 */

export type {
  Page,
  CMSImage,
  SiteSettings,
  AnalyticsSettings,
  SocialLinks,
} from "./types";

export { cms } from "./payload";
